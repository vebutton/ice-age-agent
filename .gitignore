# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# Environment / secrets
.env
.env.local*
!.env.local.example
*.env
*.pem
*.key
secrets/

# Node (if needed)
node_modules/

# OS
.DS_Store
Thumbs.db

# VSCode
.vscode/settings.json

# Project-specific local-only directories — never commit
# (customer/sample docs in collateral/; generated drafts in output/;
#  unprocessed inbound content in inbox/, including AgentMail-fetched newsletters;
#  routed deep-context files paired with CLAUDE.local.md pointers in local/)
collateral/
output/
inbox/
local/

# Atlas overview — always internal context, never commit
.atlas/

# Claude Code local/personal settings — never commit (shared settings.json is fine to track)
.claude/settings.local.json
.claude/*.local.json
*~

# Local-only files — backlog, scratch pads, meta notes — never commit
TODO.md
*-conversation.md
CLAUDE.local.md
learnings.md
